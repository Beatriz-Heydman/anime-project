export type CardAnimeProps = {
  dubOrSubText?: string;
  animeName: string;
  animeImg: string;
  subtitle: string;
};

export type StyledCardProps = {
  dubOrSubText?: string;
  animeImg: string;
};
